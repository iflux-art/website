"use client";

/**
 * 客户端初始化组件
 * 处理需要在客户端执行的初始化逻辑
 */

import { useEffect } from "react";

/**
 * 客户端初始化组件
 * 在客户端执行各种初始化任务
 */
export function InitClient() {
  useEffect(() => {
    // 客户端初始化逻辑
    // 目前没有需要初始化的内容
  }, []);

  return null; // 此组件不渲染任何UI
}
